{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Pc portable{% endblock %}
{% block body %}
<body>

 <h1 class="text-orange text-center">{{ portable.title }}</h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-10"> <!-- Utilisation de col-lg-10 pour occuper plus de 3/4 de l'écran sur les écrans larges -->
      <div class="card mb-3 bg-dark">
        <img src="{{ asset('images/' ~ portable.picture) }}" class="card-img-top" alt="...">
        <div class="card-body d-flex flex-column justify-content-center align-items-center">
          <p class="card-text text-white text-center">{{ portable.description}}</p>
          <h5 class="card-title text-white text-center fs-2">{{ portable.price }} €</h5>
          <a href="" class="btn btn-custom">Acheter</a>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
    .card {
        border: 1px solid white; /* Définir la bordure de la carte en blanc */
    }
       .btn-custom:hover {
        background-color: white; /* Couleur de fond au survol */
        color: blue; /* Couleur du texte au survol */
    }
</style>

</body>
{% endblock %}
